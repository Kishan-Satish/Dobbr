pipeline {
    agent any

    stages {

        stage('Clone Repository') {
            steps {
                checkout scm
            }
        }

        stage('Build Successful') {
            steps {
                echo 'Dobbr frontend loaded successfully!'
            }
        }

    }
}
